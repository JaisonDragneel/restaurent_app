<%= form_for @restaurent, html: { multipart: true, class: "ui container form" } do |f| %>
  <% if @restaurent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurent.errors.count, "error") %> prohibited this restaurent from being saved:</h2>

      <ul>
      <% @restaurent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit class: "ui primary button"%>
  </div>
<% end %>
